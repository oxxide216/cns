# CNS

Simple networking library written in C, providing an asynchronous interface over sockets and timers.

## Quick start

```shell
cc -o nsb nsb.c
cp build-<target-platform>.nsb build.nsb
./nsb
```

Now you have libcns.a!

## Usage

For usage, see [examples directory](./examples).
